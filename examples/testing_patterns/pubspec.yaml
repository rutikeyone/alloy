name: testing_patterns
description: How to test an app built on Alloy — overriding dependencies and the traps.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.10.0
  flutter: ">=3.38.0"

resolution: workspace

dependencies:
  alloy: ^0.1.0
  alloy_flutter: ^0.1.0
  flutter:
    sdk: flutter

dev_dependencies:
  alloy_test: ^0.1.0
  flutter_test:
    sdk: flutter
