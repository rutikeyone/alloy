/// `package:logger` adapter for Alloy.
///
/// One class: [AlloyLoggerSink], which an `AlloyLogObserver` writes through.
///
/// Not to be confused with `alloy_logging`, which adapts the *other* package —
/// the two upstream names differ by one letter and so do these.
library;

export 'package:alloy_logger/src/alloy_logger_sink.dart';
