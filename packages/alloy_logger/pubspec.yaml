name: alloy_logger
description: Alloy adapter for package:logger — sends the DI graph's events to a Logger with its pretty console output.
version: 0.1.0
repository: https://github.com/rutikeyone/alloy
issue_tracker: https://github.com/rutikeyone/alloy/issues
topics:
  - dependency-injection
  - di
  - logging
  - observability

environment:
  sdk: ^3.10.0

resolution: workspace

dependencies:
  alloy: ^0.1.0
  logger: ^2.7.0

dev_dependencies:
  test: ^1.25.0
