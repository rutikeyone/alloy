## 0.1.0

- Initial release.
- An `AlloyLogSink` writing into `package:logging`, which has no notion of a
  record type of its own.
- Note the neighbour package `alloy_logger`, one letter apart, which adapts a
  different logger.
