name: alloy_logging
description: Alloy adapter for package:logging — sends the DI graph's events to a dart.dev Logger.
version: 0.1.0
repository: https://github.com/rutikeyone/alloy
issue_tracker: https://github.com/rutikeyone/alloy/issues
topics:
  - dependency-injection
  - di
  - logging
  - observability

environment:
  sdk: ^3.13.0

resolution: workspace

dependencies:
  alloy: ^0.1.0
  logging: ^1.3.0

dev_dependencies:
  test: ^1.25.0
