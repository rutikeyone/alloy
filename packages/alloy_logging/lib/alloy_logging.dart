/// `package:logging` adapter for Alloy.
///
/// One class: [AlloyLoggingSink], which an `AlloyLogObserver` writes through.
/// Everything else — which events exist, how they are worded — lives in
/// `package:alloy`.
library;

export 'package:alloy_logging/src/alloy_logging_sink.dart';
