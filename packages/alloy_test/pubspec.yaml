name: alloy_test
description: Test helpers for Alloy — build and tear down graphs, override dependencies, and check that a hand-written graph resolves.
version: 0.1.0
repository: https://github.com/rutikeyone/alloy
issue_tracker: https://github.com/rutikeyone/alloy/issues
topics:
  - dependency-injection
  - di
  - testing
  - test

environment:
  sdk: ^3.13.0

resolution: workspace

dependencies:
  alloy: ^0.1.0
  matcher: ^0.12.16
  test_api: ^0.7.0

dev_dependencies:
  test: ^1.25.0
