/// `package:logger` adapter for Cobalt.
///
/// One class: [CobaltLoggerSink], which an `CobaltLogObserver` writes through.
///
/// Not to be confused with `cobalt_logging`, which adapts the *other* package —
/// the two upstream names differ by one letter and so do these.
library;

export 'package:cobalt_logger/src/cobalt_logger_sink.dart';
