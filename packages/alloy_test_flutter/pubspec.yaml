name: alloy_test_flutter
description: Widget-test helpers for Alloy — settle a graph that is still starting, and reach the root scope of a mounted application.
version: 0.1.0
repository: https://github.com/rutikeyone/alloy
issue_tracker: https://github.com/rutikeyone/alloy/issues
topics:
  - dependency-injection
  - di
  - testing
  - flutter

environment:
  sdk: ^3.13.0
  flutter: ">=3.47.0"

resolution: workspace

dependencies:
  alloy_flutter: ^0.1.0
  flutter:
    sdk: flutter
  flutter_test:
    sdk: flutter

dev_dependencies:
  alloy_test: ^0.1.0
